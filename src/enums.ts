export enum Labels {
  _ID = '_id',
  ID = 'Id',
  AMOUNT = 'Amount',
  DATE = 'Date',
  ACQUIRED = 'Acquired',
  NOTES = 'Notes',
  IMAGE = 'Image',
}

export enum Rating {
  ONE = 1,
  TWO = 2,
  THREE = 3,
  FOUR = 4,
  FIVE = 5,
}
